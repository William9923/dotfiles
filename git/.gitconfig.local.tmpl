# Git local config — contains user identity (not committed to public repo)
# Generated from .tmpl by setup-secrets.sh
# Variables come from zsh/.zsh_secrets

[user]
	name = ${GIT_NAME}
	email = ${GIT_EMAIL}
